# Royal SpecBoard 設計師規格工作臺

*A collaborative project by roylin1003 and Claude.*

單檔網頁版面規格工作臺。在畫布上拖出矩形版面，工程量尺即時標注尺寸與四向間距，
**每個數字都可以直接點下去改**；整張版面同步成一段規格文字（DSL）——
人看得懂、程式吃得下、AI 讀得通。要交出去的，就是那段字。

**▶ 立即使用**：<https://royal-specboard.roynexus.com/>　｜　工作臺裡按 **？** 看速查

> 目前為 **alpha**，格式與功能仍在演化中。歡迎開 Issue 回報與提案。

---

## 為什麼做這個

版面溝通的老問題：設計者說「看起來偏了」，工程師問「偏幾 px」，AI 兩邊都聽不見。
Royal SpecBoard 讓三方坐同一張桌子——

- **設計者**用眼睛與滑鼠：拖曳、吸附、對齊分布，不必看見任何座標
- **工程**用數字：選取任何元件，寬高、鄰距、邊距全部標出來，點了就能改
- **AI** 用規格文字：整張版面就是一段 DSL，貼進任何 LLM 對話就是施工單

操作即宣告：你拖到吸附，數字就寫好了；你改一個數字，畫布就動了；文字框永遠與畫布同步。

## 三分鐘上手

1. 左上角選裝置，例如「手機 390×844」。
2. 按「**排磁磚**」，畫面排好一格一格的定位點。
3. 拖方塊，或直接點尺寸數字改它。
4. 按「**複製規格**」，把下方的規格文字貼給工程師或 AI。完成。

## 常用情境

**排一個新版面**　選裝置 →「版型」選單挑一個起點，或按「排磁磚」。改「欄」的數字，磁磚即時重排。

**桌機版改成手機版**　在桌機版按「複製規格」存下 → 裝置換成手機 → 照同樣的欄、列位置把東西放回去。
磁磚的 id 是 `t列_欄`，位置用欄列講，不用像素講，換裝置就不必重想。

**一排按鈕排整齊**　框選那排 →「水平等距」；或填好「間距」按「依間距橫排」。對齊用 Alt＋A／D／W／S。

**給案主看**　按 `F` 進簡報模式，只剩畫布；再按 `F` 回來。

**跟 AI 來回改**　把 AI 回的規格貼進下方文字框 →「套用文字」。
工作臺會列出**跟上一版差在哪**（＋新增／−刪除／移動／改尺寸），看完再決定收不收；不收就 Ctrl＋Z。

## 三個概念

- **規格文字是正本。** 畫面只是確認用；交出去的是那段字。畫面和文字永遠同步。
- **磁磚是定位點。** 正方形、間距約磚的 1/10、剛好填滿畫面。橫式由欄數決定列數；
  直式由寬度決定正方形，一路往下排，最後一列吃掉剩下的高。同比例的螢幕（例如 2K 與 FHD）欄列完全相同。
- **每台裝置有預設版面。** 2K 是「2K 標準」（9×5 環狀棋盤），其他是「格式 I」（兩框 × 2×2）。
  換裝置會照新尺寸重排；「回到預設」隨時叫回來。舞台上的金色虛線框是其他裝置的大小，一眼看出現在相當於哪台。

## 本機使用

下載後**雙擊 `index.html` 就能用**（file:// 直開，零安裝、零建置、零相依）。

只有「擷取本工具介面」走 `fetch`，需要一個本機伺服器：

```bash
python -m http.server 8000
```

然後開 `http://localhost:8000/`。「開啟檔案」走瀏覽器原生的檔案選擇器（可多選），
任何位置、file:// 直開、線上版都能用。

版面每動一次就自動存進瀏覽器（localStorage），關掉再開還在；「下載 .spec.txt」把規格存成檔。
標題旁的 ● 表示「上次下載／匯出之後有改動」。

**圖即規格**：「匯出 PNG（含規格）」輸出 1:1 的畫布圖，規格文字藏在 PNG 的 tEXt 區塊裡——
那張圖貼到哪裡都帶著完整規格，**拖回工作臺就重建**。規格檔或 PNG 都可以直接拖放到畫布上載入。

## DSL 一分鐘上手

```
// 以 // 開頭的行是註解
#canvas w=2560 h=1440 bg=#0E1116 grid=8
rect id=nav x=0 y=0 w=2560 h=96 fill=#171C22
rect id=cta x=160 y=660 w=320 h=96 fill=#4CC3D9 r=48 body="開始閱讀" size=36 color=#0E1116 align=center valign=middle
```

一行一件，屬性依序：位置尺寸、`fill` 底色（`none` 為透明）、`stroke` 框線、`r` 圓角、
`op` 透明度、`body` 文字（換行寫 `\n`）與字級／字色／對齊。

它是有效的 AI 交換格式：把這段字貼給任何 LLM，它讀得懂你的版面；
請它回一段 DSL，貼進「套用文字」，你就看得見它的提案——套用後會列出**跟上一版的差異**
（＋新增／−刪除／移動／改尺寸／改樣式，點開看細節），貼進來、看差異、決定收不收。

## 實況擷取器

`snapshot-capture.js`——把**任何跑著的網頁**抄成 DSL：
開那個頁面的 DevTools console，整檔貼上按 Enter，規格文字就進了剪貼簿。
拿真實專案的現況進工作臺診斷、標注、改版，這是第一步。

`samples/` 附兩份擷取範例，下載 repo 後用「開啟檔案」開即可。

## 快捷鍵

| 按法 | 做什麼 |
|---|---|
| 拖曳 ／ Alt＋拖曳 | 移動（自動吸附）／拖出一份複製 |
| Shift＋點選 ／ 空白處拖曳 | 加減選取 ／ 框選 |
| 方向鍵 ／ Shift＋方向鍵 | 移 1px ／ 移一格 |
| `[` `]` ／ Shift＋`[` `]` | 寬度 −＋一格 ／ 高度 −＋一格 |
| `D` ／ Delete | 複製 ／ 刪除 |
| Ctrl＋C ／ X ／ V | 複製／剪下／貼上（剪貼簿裡是規格文字，可跨分頁、可貼給 AI） |
| Ctrl＋] ／ Ctrl＋[ | 圖層上移／下移 |
| Alt＋A D W S ／ Alt＋H V | 靠左右上下 ／ 水平、垂直置中 |
| `I` | 滴管：吸螢幕上任何顏色，套到選取物件（Chrome／Edge） |
| 滾輪 ／ Shift＋滾輪 ／ Ctrl＋滾輪 | 上下捲 ／ 左右捲 ／ 縮放 |
| `0` ／ Shift＋1 ／ Shift＋2 | 整張 ／ 整張 ／ 縮放到選取 |
| 中鍵拖曳 ／ 按住空白鍵拖曳 | 平移 |
| `F` | 簡報模式 |
| `?` | 速查 |
| Ctrl＋Z ／ Ctrl＋Shift＋Z | 復原 ／ 重做 |
| 點量尺數字 | 原地改值（間距數字＝移動元件） |
| 數字欄打 `+16`、`*2`、`358/2` | 接原值計算；▲▼ 或 ↑↓ 微調（Shift ×10） |
| 拖放檔案到畫布 | 載入 .spec.txt／.png（含規格） |

有 [TourBox](https://www.tourboxtech.com/) 的話，[TOURBOX.md](TOURBOX.md) 有整套按鍵對照：左手轉旋鈕調尺寸，右手滑鼠拖。

## 參與開發

歡迎程式設計師加入——這個 repo 的參與門檻刻意壓到最低：
單一 HTML 檔、無框架、無建置、clone 下來改完重整就看得到。

- **Issue**：bug、需求、設計討論都收
- **Pull Request**：直接改 `index.html` 送過來
- **用 DSL 提案**：要建議 UI 改動，可以直接在工作臺裡畫好、貼譜面到 Issue——
  這個工具自己的輸出格式就是它的提案格式

### Roadmap

- **多頁規格**：一份 DSL 含桌機＋手機兩張畫布，同 id 元件跨頁聯動選取、輸出映射報告
- **分頁**：一個工作臺管多張畫布
- **約束詞彙**：DSL 記「吸附／等寬／貼邊」等關係，不只記座標

## 授權

MIT，詳見 [LICENSE](LICENSE)。
