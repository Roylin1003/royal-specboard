# TourBox × SpecBoard 對照表

TourBox 的原理：每個旋鈕、按鍵都送出**鍵盤快捷鍵**（或滾輪）。所以不用裝任何東西，
只要在 **TourBox Console** 替瀏覽器（Chrome／Edge）建一個預設，把下面的鍵填進去。

> 做法：TourBox Console →「預設」→ 新增 → 綁定應用程式選 `chrome.exe`（或 `msedge.exe`）→
> 照下表逐格設定。只在瀏覽器前景時生效，不影響其他軟體。

## 建議配置（右手滑鼠、左手 TourBox）

| TourBox 控制 | 轉／按 | 送出 | SpecBoard 做什麼 |
|---|---|---|---|
| **Knob 旋鈕** | 順／逆 | `]` ／ `[` | 選取物件寬度 ＋／− 一格 |
| Knob ＋ 按住 Tall | 順／逆 | `Shift+]` ／ `Shift+[` | 高度 ＋／− 一格 |
| **Scroll 滾輪** | 上／下 | `↑` ／ `↓` | 上下移 1px |
| Scroll ＋ 按住 Tall | 上／下 | `Shift+↑` ／ `Shift+↓` | 上下移一格 |
| **Dial 轉盤** | 順／逆 | `→` ／ `←` | 左右移 1px |
| Dial ＋ 按住 Tall | 順／逆 | `Shift+→` ／ `Shift+←` | 左右移一格 |
| **Tall** | 按住 | （修飾鍵，同 Shift） | 讓上面三個變「一格」 |
| **Short** | 按 | `Ctrl+Z` | 復原 |
| Short ＋ Tall | 按 | `Ctrl+Shift+Z` | 重做 |
| **Top** | 按 | `Shift+2` | 縮放到選取 |
| **Side** | 按 | `Shift+1` | 回整張 |
| **C1** | 按 | `D` | 複製一份 |
| **C2** | 按 | `Delete` | 刪除 |
| **Tour** | 按 | `F` | 簡報模式（只留畫布） |
| 方向鍵 ↑ ／ ↓ | 按 | `Ctrl+]` ／ `Ctrl+[` | 圖層上移／下移 |
| 方向鍵 ← ／ → | 按 | `Alt+A` ／ `Alt+D` | 靠左／靠右對齊 |

縮放不用設：滑鼠 **Ctrl＋滾輪** 就是縮放（也可以把 Scroll＋Side 設成 `Ctrl+滾輪`）。

## 全部快捷鍵（SpecBoard 2026-09-23）

| 鍵 | 功能 | 借自 |
|---|---|---|
| 方向鍵 ／ Shift＋方向鍵 | 移 1px ／ 移一格 | — |
| `[` `]` ／ Shift＋`[` `]` | 寬 −＋一格 ／ 高 −＋一格 | Photoshop 筆刷大小 |
| `D` ／ Alt＋拖曳 | 複製一份 ／ 拖出一份 | Figma、PowerPoint（Ctrl＋拖曳） |
| Shift＋拉角 | 保持比例縮放 | Figma、Photoshop |
| Ctrl＋C ／ X ／ V | 複製／剪下／貼上（剪貼簿裡是規格文字，可跨分頁、可貼給 AI） | 全部 |
| Ctrl＋] ／ Ctrl＋[ | 圖層上移／下移 | Figma、PowerPoint |
| Alt＋A D W S | 靠左／右／上／下 | Figma |
| Alt＋H ／ Alt＋V | 水平置中／垂直置中 | Figma |
| `I` | 滴管（吸螢幕上任何顏色，套到選取物件） | Photoshop、Figma |
| Ctrl＋滾輪 ／ 滾輪 ／ Shift＋滾輪 | 縮放 ／ 上下捲 ／ 左右捲 | Figma |
| Shift＋1 ／ `0` ／ Shift＋2 | 整張 ／ 整張 ／ 縮放到選取 | Figma |
| `F` | 簡報模式 | PowerPoint 投影片放映 |
| 數字欄打 `+16`、`*2`、`358/2` | 接原值計算 | Figma |
| Ctrl＋Z ／ Ctrl＋Shift＋Z | 復原／重做 | 全部 |
| 工具列「欄」＋「磁磚」 | 排磁磚矩陣（定位點）；在磁磚模式時改「欄」即時重排 | Roy 的 2K 標準／手機解法 |
